{
  "name": "lets-go",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "backend:lint": "cd backend && golangci-lint run",
    "backend:dev": "cd backend && air",
    "frontend:start": "cd frontend && nodemon dev-server.js",
    "prisma:migrate": "cd backend && npx prisma migrate dev"
  },
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
